# Kernels these modules are known to be correct for, and which set each one
# needs. One entry per line:
#
#     <uname -r> <uname -v>|<directory>
#
# The directory comes last, after a pipe, because the identity contains runs of
# spaces that a shell would collapse: a single-digit build day gives "Jun  5",
# and splitting on whitespace would quietly stop matching it.
#
# WHY THIS FILE EXISTS. The module loader compares a "vermagic" string, and
# vermagic carries only the kernel version, SMP, preemption, module-unload
# support and the architecture. It says nothing about the rest of the
# configuration. Two different builds of 4.14.14-funkey can therefore share a
# vermagic exactly while disagreeing about the layout of struct page, of a
# spinlock, or of the mm internals that zram and zsmalloc reach into. insmod
# would accept such a module and the result would be memory corruption rather
# than a clean refusal - the one failure the loader cannot protect anyone from.
#
# So the launcher refuses to load anything into a kernel that is not named here.
# It is a whitelist of builds somebody has actually verified, not a guess.
#
# ADDING YOURS. You do not have to collect anything by hand: when the launcher
# refuses an unknown kernel it writes `nanocraft-kernel.txt` to the root of the
# SD card, carrying the identity line, the configuration where the build exposes
# it, and the kernel's full symbol table. That table is what matters - every
# symbol these modules import can be checked against the real kernel before
# anything is loaded on the hardware. Rebuilding needs no firmware change. See
# README.md in this directory.
#
# Note this is deliberately not /proc/version verbatim: that line carries the
# username and hostname of whoever built the kernel, which is nobody's business.

# Factory DrUm78 RG Nano, FunKey-OS 2.3.0 "Zen Zebu". Its kernel is SMP.
#
# The card image carries two rootfs slots whose kernels were built sixteen
# minutes apart and are not the same build - slot 2 is over a megabyte larger.
# Slot 2 is what a factory console actually boots, and it is the one listed
# here. Slot 1 (03:01:26) passes the same export audit but nothing has been
# seen running it, so it stays off the list until something is.
4.14.14-funkey #1 SMP Sun Jan 18 03:17:24 CET 2026|smp

# Factory FunKey S, FunKey-OS 2.3.0. Reported in issue #2 by badcats72, who
# sent the kernel image this was audited against.
#
# A THIRD build of the same source, 44 minutes after the RG Nano's: same
# compiler, same Buildroot revision, same builder, same day. Audited from their
# own zImage rather than assumed:
#
#   - all 158 undefined symbols across the five modules resolve against its
#     export table;
#   - its export table is a strict SUBSET of the verified RG Nano kernel's -
#     nothing is exported here that is not exported there;
#   - the 27 exports it lacks are all USB MIDI and USB host
#     (snd_rawmidi_*, snd_usbmidi_*, musb_root_disconnect), which zram,
#     zsmalloc and lz4 do not touch;
#   - every config option that would move struct page, a spinlock, a zone or
#     the allocator is exported identically by both.
#
# See docs/kernel-audits/funkey-s/. What has NOT happened is somebody watching
# it run, which is the bar slot 1 above is still waiting on. It is listed
# anyway, because the person who reported it cannot verify it while the
# launcher refuses to load anything - and they are the one console that can.
4.14.14-funkey #1 SMP Sun Jan 18 03:45:29 CET 2026|smp

# The console this port was developed on: a custom build, uniprocessor.
4.14.14-funkey #1 Thu Jun 18 07:57:19 CEST 2026|up
